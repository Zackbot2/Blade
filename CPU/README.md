# CPU
This is where the actual game files are stored (or.. backed up, I guess). `circuit.data` is the data of the CPU, with `65536_*.bin` belonging to each SSD stored.

These belong at `\AppData\Roaming\Godot\app_userdata\Turing Complete\schematics\architecture\<SCHEMATIC NAME>`